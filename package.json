{
  "name": "server",
  "version": "1.0.0",
  "description": "Luke Houge Portfolio",
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint src/**",
    "test": "jest",
    "coverage": "jest --collectCoverageFrom=src/**.js --coverage",
    "coveralls": "jest --coverage && cat ./tests/coverage/lcov.info | coveralls"
  },
  "author": "Luke Houge",
  "license": "ISC",
  "devDependencies": {
    "coveralls": "^3.0.3",
    "jest": "^24.1.0"
  }
}
